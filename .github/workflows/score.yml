name: Scoring code quality

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  score-code:
    runs-on: ${{ matrix.os }}-latest
    permissions:
      contents: read
      pull-requests: write
    defaults:
      run:
        shell: bash -l {0}
    strategy:
      fail-fast: false
      max-parallel: 5
      matrix:
        os: ["ubuntu"]
    continue-on-error: true

    steps:

    - uses: actions/checkout@v4

    - name: Set up micromamba
      uses: mamba-org/setup-micromamba@v2
      with:
        micromamba-version: "2.0.2-2"
        environment-file: environment.yml
        environment-name: amls2-final
        cache-environment: false
        cache-downloads: false

    - name: Show interpreter (sanity check)
      shell: bash
      run: |
        micromamba run -n amls2-final python -V
        micromamba run -n amls2-final which python        

    - name: Install extra tools inside the env (grader deps)
      shell: bash
      run: |
        micromamba run -n amls2-final python -m pip install --upgrade pip
        ls -la .
        # If requirements.txt exists, install it INTO THE ENV
        if [ -f requirements.txt ]; then
          micromamba run -n amls2-final python -m pip install -r requirements.txt
        fi
        # Grader's own deps (kept minimal)
        micromamba run -n amls2-final python -m pip install pylint
        
    - name: Check code quality
      id: linting
      run: |
        MESSAGE=$(pylint -ry $(git ls-files '*.py') ||:)

        echo 'MESSAGE<<EOF' >> $GITHUB_ENV
        echo "<pre><code>$MESSAGE</code></pre>" >> $GITHUB_ENV
        echo 'EOF' >> $GITHUB_ENV

        echo "Printing PR message: $MESSAGE"

    - name: Post report to pull request
      uses: mshick/add-pr-comment@v2
      with:
        message: ${{ env.MESSAGE }}
        repo-token: ${{ secrets.GITHUB_TOKEN }}

    - name: Check for reproducibility
      run: |
        micromamba run -n amls2-final python main.py
